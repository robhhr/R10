import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  safeAreaView: {
    flex: 1,
    // minHeight: '100%',
  },
  scroll: {
    minHeight: '100%',
  },
  wrapperContainer: {
    marginHorizontal: 20,
  },
})

export default styles
