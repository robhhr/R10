import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  safeAreaView: {
    flex: 1,
  },
  wrapperContainer: {
    marginVertical: 10,
    marginHorizontal: 20,
  },
})

export default styles
